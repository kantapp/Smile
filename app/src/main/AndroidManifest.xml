<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kantapp.smile">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".FirstScreen"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.fullScreen" />
        <activity
            android:name=".SignUpActivity"
            android:label="SIGN UP" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:label="FORGOT PASSWORD" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.fullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.ProfilePicChange" />
        <activity android:name=".Activity.NameChangeActivity" />
        <activity android:name=".Activity.PasswordChangeActivity" />
        <activity android:name=".Activity.NewStoryActivity"></activity>
    </application>

</manifest>